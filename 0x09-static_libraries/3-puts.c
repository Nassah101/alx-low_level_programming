#include "main.h"
/**
* _puts - check the code for main
* @str: string to be printed out
* Return: always 0
*/
void _puts(char *src)
{
int i;
for (i = 0; str[i] != '\0'; i++)
_putschar(str[i]);
_putchar('\n');
}
